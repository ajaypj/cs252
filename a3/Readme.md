# Create blank Ionic project

```ionic start blank```

# Add the pages folder to the new project

# Install dependancies npm install <dependancy> --save, dependancies are mainly leaflet, angular core, and geolocation

# To run on localhost:

ionic serve

# check if debugging is on
adb devices

ionic run android

export ANDROID_HOME=/<installation location>/android-sdk-linux
export PATH=${PATH}:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools
